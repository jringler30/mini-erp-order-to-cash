# run_project.py
# Entry point to build the database and load all data from scratch.
# Run this once before launching the dashboard.

# TODO: import and call scripts in order:
# 1. create_database.py  — creates tables from schema.sql
# 2. generate_fake_data.py — generates fake records
# 3. load_data.py         — inserts records into erp.db

print("run_project.py — not yet implemented")
